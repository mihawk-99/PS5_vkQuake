diff --git a/patches/series b/patches/series
index 42fba5a..f06cf9c 100644
--- a/patches/series
+++ b/patches/series
@@ -34,3 +34,6 @@
 
 # 0055  convert the RGUI producer RGBA4444 channels to full-range RGBA8888
 0055-correct-rgui-rgba8888-upload
+
+# 0056  diagnose mixed opaque/blended driver state with equivalent ONE/ZERO overwrite
+0056-explicit-stock-pass-blend-state
diff --git a/tests/test_frontend.py b/tests/test_frontend.py
index 399aac2..f757b48 100644
--- a/tests/test_frontend.py
+++ b/tests/test_frontend.py
@@ -300,7 +300,7 @@ class PortPatches(unittest.TestCase):
         change deliberately, in a commit that says why.
         """
         self.assertEqual(
-            len(self.blocks()), 68,
+            len(self.blocks()), 69,
             "the patch count changed: if a block was added or removed on purpose, "
             "update this number in the same commit and say why in its message")
 
diff --git a/tools/apply-port-patches.py b/tools/apply-port-patches.py
index 0e789a8..79aba02 100755
--- a/tools/apply-port-patches.py
+++ b/tools/apply-port-patches.py
@@ -1958,6 +1958,20 @@ EDITS = [
         "                  | ((pix & 15u) << 24)) * 17u;\n",
         "patches/series, 0055",
     ),
+    (
+        "gfx/drivers_shader/shader_vulkan.cpp",
+        "   blend_attachment.blendEnable                 = VK_FALSE;\n",
+        "   /* Added by this port (patches/series, 0056): equivalent opaque overwrite\n"
+        "    * with explicit blend state and the driver's FP16 colour export path. */\n"
+        "   blend_attachment.blendEnable                 = VK_TRUE;\n"
+        "   blend_attachment.srcColorBlendFactor         = VK_BLEND_FACTOR_ONE;\n"
+        "   blend_attachment.dstColorBlendFactor         = VK_BLEND_FACTOR_ZERO;\n"
+        "   blend_attachment.colorBlendOp                = VK_BLEND_OP_ADD;\n"
+        "   blend_attachment.srcAlphaBlendFactor         = VK_BLEND_FACTOR_ONE;\n"
+        "   blend_attachment.dstAlphaBlendFactor         = VK_BLEND_FACTOR_ZERO;\n"
+        "   blend_attachment.alphaBlendOp                = VK_BLEND_OP_ADD;\n",
+        "patches/series, 0056",
+    ),
 ]
 
 
